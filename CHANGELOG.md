# Changelog

All notable changes to this project will be documented in this file.

## 0.2.0

- Initial open-source release.
- Safe wrapper around `adb` + `scrcpy`.
- Optional NL planning/execution via `llm_task`, with schema validation and confirmation token flow.
- Allowlist enforcement for `open_app`, URL scheme checks, coordinate bounds checks.
